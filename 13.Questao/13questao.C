#include <stdio.h>
#include <math.h> 

int main(){

    float i, j, num, res, a, b;

    printf("Insira um numero:");
    scanf("%f", &num);

    for (i = 25; i >= 1; i--){

        j++
        printf("Valor i %f\n", i);
        printf("Valor j %f\n", j);

        res = res+ (pow(num, ))/n;
        printf("%f\n", res);
    }

    printf("\nO resultado eh %f", res);

    return 0;
}